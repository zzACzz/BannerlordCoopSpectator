namespace CoopSpectator.Network.Messages // Оголошуємо простір імен для мережевих повідомлень/моделей
{ // Починаємо блок простору імен
    /// <summary> // Документуємо клас
    /// Список префіксів (типів) повідомлень у нашому простому текстовому протоколі поверх TCP. // Пояснюємо призначення
    /// </summary> // Завершуємо XML-коментар
    internal static class NetworkMessagePrefixes // Оголошуємо static клас, бо це просто константи
    { // Починаємо блок класу
        internal const string State = "STATE:"; // Префікс для повідомлення зі станом хоста на мапі кампанії
        internal const string BattleStart = "BATTLE_START:"; // Префікс для повідомлення про старт битви (потрібно буде на наступних етапах)
        internal const string BattleEnd = "BATTLE_END:"; // Префікс для повідомлення про кінець битви (потрібно буде на наступних етапах)
    } // Завершуємо блок класу
} // Завершуємо блок простору імен

