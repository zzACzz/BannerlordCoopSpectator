# Mount & Blade II: Bannerlord - Coop Campaign

Current architecture source of truth: `docs/COOP_RUNTIME_MAP.md`

Project goal:

- host keeps running the normal campaign;
- remote players join battles through multiplayer;
- battle results return back into campaign state.

Operational docs that still matter:

- `docs/COOP_RUNTIME_MAP.md` for current architecture, startup/join contract, legacy status, and transition plan
- `BUILD_RUNBOOK.md` for build and run steps
- `docs/archive/` only for archaeology
