# Battle Test Automation Client Join Implementation Report

Status: **Exact server selection, native join, authoritative handoff, and terminal Connected live-confirmed**

Implementation date: **2026-08-31**

Latest source/contract correction date: **2026-09-01**

Original repository base revision: **`3c513084ebbe9c99daa0b65849fab7b39b913ee1`** (`3c51308`)

Committed implementation revision: **`f91eeff9b710f68fc7bf4b506ec39c2d1c4474bc`** (`f91eeff`)

Published post-start ownership revision: **`711f2cac20ca05d3e81233aa6acfa60816dcce99`** (`711f2ca`)

Published server-selection correction revision: **`e55f1bd`**

Published handoff observer correction revision: **`d1af692`**

Published timeout-evidence correction revision: **`3fdcda3`**

Clean published staging revision: **`a7bd5285902d30793d0908186fb392fe537cf314`** (`a7bd528`)

Formal connection live-proof revision: **`d0648edf2f5033ec1ff48efe547a645c587e2ca1`** (`d0648ed`)

Current specification: [BATTLE_TEST_AUTOMATION_SPEC.md](BATTLE_TEST_AUTOMATION_SPEC.md), Revision 16. The original client-join slice was implemented before the Revision 7 runtime-safety foundation; the post-start ownership handoff was hardened against the Revision 10 process contract. Revision 11 records that the dedicated starter's redirected standard handles are not a sufficient live control/evidence channel. Revision 12 incorporates the first real client launch and corrects the aggregate handoff's JSON-date and retained-ownership defects. The subsequent clean rerun exposed a source implementation defect rather than a specification change: the reflection adapter searched the correct `TaleWorlds.MountAndBlade.NetworkMain` type name in the wrong assembly. Revision 13 records the corrected-client live result and bounded native platform-login gate. Revision 14 records the successful native-login/server-list run and the correction that separates scene `Map` from native serialized `UniqueMapId`. Revision 15 records exact native join success and moves formal handoff observation to the actual `GameNetwork.StartMultiplayerOnClient(...)` boundary. Revision 16 records the passing terminal connection proof and the remaining broader Milestone 2B safety gaps.

## 1. Outcome

The Milestone 1 normal-lobby control blocker now has a narrow source implementation, isolated contract coverage, and a passing live proof. A dedicated launcher validates Steam and the exact installed client module, creates a fresh run-scoped join request, launches the real multiplayer executable, and lets the module discover and request the associated local server through TaleWorlds' normal custom-game lobby APIs.

Implementation validation itself launched no product process. A later controlled operation installed exact `0.3.2` binaries from committed revision `f91eeff` and proved their on-disk paths/hashes. Clean run `m2b2d-live-feasibility-20260901-01` subsequently launched the real client from published revision `2104295`; the client published the selected loaded hash and reached `WaitingForLobby`, while the aggregate runner exposed the corrected UTC/ownership work. Clean Revision 12 rerun `m2d-live-r2-01` then proved schema-v4 identity import, retained automatic cleanup authority, the complete dedicated bootstrap, UDP ownership, and exact client loaded identity. It stopped at `RequestExpired` because `CoopLobbyAutomationDriver` searched assembly `TaleWorlds.MountAndBlade.Multiplayer` for `TaleWorlds.MountAndBlade.NetworkMain`; ILSpy proved that the installed `1.4.8` type is defined in `TaleWorlds.MountAndBlade.dll`.

Published-revision build `m2d-r13-pub-b1` produced corrected client SHA-256 `1C500501CE25D4A520782F61B338F9D8D0A4C591A4748E80991A521B63379250`. A low-level comparison against the earlier `2437...6887` candidate found 620/620 decompiled C# files identical; only generated reference-path metadata differed, so the fresh published artifact was selected. Transaction `m2d-client-stage-r2` retained the complete previous 32-file client tree and installed only the changed DLL/PDB pair. Live run `m2d-live-r3-01` then loaded the exact corrected hash and advanced the controller to `WaitingForLobby` with `LobbyState=Idle`, proving that the exact-assembly resolver works inside the game.

The same run exposed the next prerequisite: the exact client displayed the stock `Not Logged In` screen and never reached `AtLobby`. Its PID-correlated native log showed `NetworkMain Initialized`, `MultiplayerLobbyGauntletScreen::HandleActivate`, and failed native REST setup before the run-scoped request expired. ILSpy inspection of the installed runtime showed that stock `MPAuthenticationVM.ExecuteLogin()` delegates to public `LobbyState.TryLogin()`, which performs the platform privilege checks and creates the native lobby login provider. Revision 13 implemented and staged that exact one-shot path. Clean run `m2e-live-r1-01` then live-confirmed successful native platform login, `AtLobby`, and 52 successful custom-server-list responses. No join followed because no entry passed every requested selector. The retained artifacts did not capture list entries, so they cannot prove server absence. A post-run installed-runtime audit found that the runner incorrectly passed scene `Map=mp_tdm_map_001` as `UniqueMapId`, although the native field contains a separately serialized `UniqueSceneId` or null. Published runner-only correction `e55f1bd` omits the unavailable optional filter and passed focused contracts plus canonical run `m2e1-c1` at 22/22 without a product build, launch, installed-module mutation, or staging operation.

Clean corrected-selection run `m2e1-live-r1-01` selected the exact run-owned server, completed `RequestJoinCustomGame(...)`, executed the real `GameNetwork.StartMultiplayerOnClient(...)` path with one-shot loopback rewrite, entered `InCustomGame`, logged `Join game successful`, received `CreatePlayer`, and visibly displayed `Awaiting Server`. The formal status nevertheless remained non-terminal `JoinAccepted`, and the runner returned `Timeout`. Installed-runtime IL inspection proved that the notification lived only in a historical four-argument `LobbyGameStateCustomGameClient.StartMultiplayer` reflection patch; installed `1.4.8` instead exposes a parameterless override that calls `GameNetwork.StartMultiplayerOnClient(...)`. Published corrections `d1af692` and `3fdcda3` fix the observer boundary and timeout evidence. Full contracts passed 22/22. Clean published compile `m2e1-handoff-pub-compile-20260901-01` from `a7bd528` and controlled client-only transaction `m2e1-handoff-stage-r1` installed corrected client SHA-256 `7CC2D759806D2F02D8BEBA15BCBC01EF61DDE4975004728F3D7E6C8332977E97` with the complete prior 32-file tree retained. Clean run `m2e1-handoff-live-r1-01` loaded that exact client, observed post-rewrite `NetworkHandoff`, reached terminal `Connected`, returned `Pass`, retained native join success plus server `CreatePlayer`, and completed exact graceful cleanup with the protected result unchanged. No campaign, cooperative mission, or battle was launched. See [BATTLE_TEST_AUTOMATION_M2B2_STAGING.md](BATTLE_TEST_AUTOMATION_M2B2_STAGING.md), [BATTLE_TEST_AUTOMATION_M2B2_FEASIBILITY.md](BATTLE_TEST_AUTOMATION_M2B2_FEASIBILITY.md), and [BATTLE_TEST_AUTOMATION_M2B2D_DEDICATED_CONTROL.md](BATTLE_TEST_AUTOMATION_M2B2D_DEDICATED_CONTROL.md).

## 2. Implemented boundary

### Launcher

- `run_battle_test_client.bat` is retained as the historical standalone wrapper. Current source permits that path only for validation; a live client must inherit exact server ownership through the aggregate `Feasibility` runner.
- `scripts/Start-CoopBattleTestClient.ps1` validates the `RunId`, server identity, Steam process, Bannerlord executable, module descriptor, module DLL, bundled Harmony DLL, and the expected installed module SHA-256 before process creation.
- `-ValidateOnly` performs those checks without creating a run directory or starting Bannerlord.
- A live call without `-UseExistingRunContract` fails before creating the run root because a standalone client cannot prove exact dedicated-process ownership.
- A server password is accepted only from `COOPSPECTATOR_AUTOMATION_SERVER_PASSWORD` in the launcher environment. It is never accepted as a command-line argument or written to the request, status, launch artifact, or logs.
- A real launch creates a fresh request, provisional launch artifact, verified final launch artifact, and exclusive launcher lock below `%TEMP%\CoopSpectator\Automation\<RunId>`. Existing request or status files cause rejection.
- Immediately after process creation, the launcher binds a provisional identity to the exact PID, requested executable path, aggregate-runner parent PID, launch window, and launch-operation ID. It uses bounded `Process`/`Win32_Process` observation before publishing schema-v4 `client-launch.json` with the complete verified identity, including executable hash and the original launch evidence.
- The aggregate validates and records that complete identity before fallible live revalidation. JSON UTC fields are normalized directly from either strings or `System.DateTime` values; no culture-dependent intermediate string is permitted.
- Any exception after process creation but before final artifact publication triggers PID-reuse-resistant exact cleanup and propagates `RunnerInternalError`. The final atomic artifact is the handoff boundary after which the aggregate runner owns cleanup.
- The child receives the test flag, `RunId`, exact run root, un-hashed run token, and optional password through its environment. The persisted request contains only the token hash.

### Module control path

- `Infrastructure/ExperimentalFeatures.cs` keeps the path disabled unless `COOPSPECTATOR_TEST_AUTOMATION=1` is explicitly set.
- `Infrastructure/Automation/CoopAutomationJoinContract.cs` validates schema 3, run identity, sequence, command ID, bounded lifetime, token hash, loaded module hash, and exact server selectors. Scene `Map` and native serialized `UniqueMapId` are distinct fields; the optional unique-map selector is omitted when no authoritative serialized value is available.
- Join request schema 3 uses protocol 1.0 and exact `Runner/runner-01 -> MultiplayerClient/multiplayer-client-01` source/target identity. It shares the general run correlation constants added by Milestone 2A.
- `Infrastructure/Automation/CoopAutomationJoinBridge.cs` restricts the initial profile to the expected temporary run root, hashes the actually loaded assembly, reads the request, and publishes strictly atomic status including `PlatformLoginAttempted`, `PlatformLoginTaskState`, and `PlatformLoginOutcome`.
- `Multiplayer/Automation/CoopLobbyAutomationDriver.cs` resolves `TaleWorlds.MountAndBlade.NetworkMain` from its exact defining assembly, `TaleWorlds.MountAndBlade`, and the exact installed `TaleWorlds.MountAndBlade.LobbyState` from `TaleWorlds.MountAndBlade.Multiplayer`. It resolves `Game.Current.GameStateManager.ActiveState`, requires exact state type and lobby-client reference equality, invokes public parameterless `TryLogin()` at most once, and otherwise reflects only the current TaleWorlds lobby surface needed for `GetCustomGameServerList()` and `RequestJoinCustomGame(...)`. Structural and state failures remain distinct evidence.
- `Multiplayer/Automation/CoopLobbyAutomationController.cs` runs from the existing application tick, retains and observes the native login task, prevents a second attempt or false cancellation after TaleWorlds owns it, selects exactly one matching server, verifies local-host association, requests the native join, and records state changes. Current automation validates a run-scoped token-bound owned-host record, exact live process identity, and active UDP port; it neither trusts nor mutates the production persisted host marker.
- `Patches/LocalJoinAddressPatch.cs` patches the actual `GameNetwork.StartMultiplayerOnClient(...)` boundary, applies the one-shot same-machine loopback rewrite, and then reports the final address plus native handoff arguments to the controller. `Patches/LobbyCustomGameLocalJoinPatch.cs` remains an optional version-specific reflection rewrite boundary and does not duplicate handoff notification.
- `Commands/CoopAutomationConsoleCommands.cs` exposes `coop.automation_join status`, `coop.automation_join start <RunId>`, and safe pre-join cancellation. The launcher request itself arms the path; the command is an explicit observation/control surface, not a second connection implementation.

### State and deadline semantics

The status path is `state/client-join.status.json`. States distinguish `ModuleReady`, `WaitingForLobby`, `RequestingPlatformLogin`, `WaitingForPlatformLogin`, `PlatformLoginSucceeded`, `WaitingForLobbyAfterPlatformLogin`, `RequestingServerList`, `WaitingForServer`, `JoinRequested`, `JoinAccepted`, `NetworkHandoff`, `Connected`, `Failed`, and `Cancelled`.

Request expiry prevents a new native login or join task from starting. Once TaleWorlds owns either task, the module does not claim cancellation or terminal expiry because the operation may still complete. The external runner imposes its bounded connection timeout and cleans up only the exact process it owns. If that timeout follows a valid non-terminal status, the final report retains that exact status with `IsTerminal=false`; evidence retention never converts a timeout into a pass.

The launcher and client module do not issue `start_game`, fabricate battle readiness, or use UI automation. Revision 11 keeps the external runner as the sole bootstrap authority after loaded-role and result-suppression gates, but requires a separate run-scoped dedicated command-intent and acknowledgement channel before the minimum vanilla `TeamDeathmatch` bootstrap may be trusted.

Milestone 2A subsequently supplied the general run manifest, nonce fingerprint, role-instance, lease, event, outcome, assertion, file-fault, and compile-only foundation documented in [BATTLE_TEST_AUTOMATION_M2A_IMPLEMENTATION.md](BATTLE_TEST_AUTOMATION_M2A_IMPLEMENTATION.md). This closes the general non-runtime protocol/build prerequisites but does not change the unverified live-join status of this client slice.

## 3. Validation evidence

The original slice used only the isolated `net8.0` contract project because normal game-project targets deploy into installed module trees. Current corrections also pass the dedicated `CoopCompileOnly=true` build plane, which redirects every output below the run root and disables installation deployment.

| Check | Command or evidence | Result |
|---|---|---|
| Runtime automation source compilation plus contract behavior | `dotnet run --project .\Tests\CoopAutomationJoin.ContractTests\CoopAutomationJoin.ContractTests.csproj --configuration Release` | Passed: `Coop automation join contract tests passed.` |
| PowerShell 7 syntax | `pwsh.exe` parsed `scripts/Start-CoopBattleTestClient.ps1` through `ScriptBlock.Create` | Passed |
| Windows PowerShell 5.1 syntax | `powershell.exe` parsed the same script through `ScriptBlock.Create` | Passed |
| Wrong installed hash rejection | `Start-CoopBattleTestClient.ps1 ... -ExpectedClientModuleSha256 A...A -ValidateOnly` | Rejected with exit code `1`; no run root was created |
| Historical installed-profile validation before current staging | `Start-CoopBattleTestClient.ps1 -RunId M2B-installed-validate-only -ServerName AC_COOP -ExpectedClientModuleSha256 9B271E4E0CFA3AD0FF2DB4B3ACC5A69AE6405E833D52ECB4E1A4C0FDCA8C1B31 -ValidateOnly` | Passed against the then-installed `0.3.1`; Steam found; no game launch |
| BAT argument guard | `cmd.exe /d /c run_battle_test_client.bat` | Usage shown; exit code `2`; no game launch |
| Patch whitespace/error check | `git diff --check` | Passed |
| Full canonical contract inventory after protocol 1.0 integration | `Invoke-CoopTest.ps1 -Command Contracts -RunId m2a-contracts-20260831-07 -All` | Passed 20/20; no product process launched |
| Main client/dedicated compile-only proof | `Invoke-CoopTest.ps1 -Command CompileOnly -RunId m2a-compile-only-20260831-03` | Passed; installed module inventories unchanged; no product process launched |
| Clean committed-revision compile and controlled install | `m2b-prestage-compile-20260831-01`, `m2b-install-20260831-01` | Exact `0.3.2` client/dedicated hashes installed; full `0.3.1` pre-images retained; no product process launched |
| Post-install environment doctor | `m2b-postinstall-doctor-20260831-01` | Installed/repository hashes match; only `RuntimeVersionCombinationNotYetVerified` remains |
| Milestone 2B.1 final contracts and compile-only | `m2b1-final-contracts-20260831-03`, `m2b1-final-compile-only-20260831-02` | Passed 21/21 and both main projects; installed inventories unchanged; no product process launched |
| Post-start handoff focused contracts | `Tests/CoopAutomationRunner.ContractTests` under Windows PowerShell 5.1 and PowerShell 7 | Passed source-order, exact parent/path/start, PID-reuse rejection, synthetic post-start failure, and provisional cleanup coverage |
| Current installed-profile validation | `m2b2c-client-handoff-validate-20260831-01` with `-ValidateOnly` | Exact installed client `0.3.2` hash `B576...7928` validated; Steam found; no run root or product process created |
| Client-handoff full canonical inventory | `m2b2c-client-handoff-contracts-20260831-01` | Passed 22/22; no product process launched |
| Client-handoff compile-only proof | `m2b2c-client-handoff-compile-20260831-01` | Both builds passed; installed inventories unchanged; no product process launched |
| Published ownership revision | `711f2cac20ca05d3e81233aa6acfa60816dcce99` | Client-launcher handoff correction committed and pushed before the clean live attempt |
| Clean live dedicated gate | `m2b2c-client-handoff-live-20260831-01` | Dedicated ownership/cleanup passed; redirected readiness timed out before client launch; installed hashes and protected result unchanged |
| First real client launch | `m2b2d-live-feasibility-20260901-01` | Exact dedicated control, seven acknowledgements, UDP ownership, real client creation, selected client loaded hash, and `WaitingForLobby` confirmed; aggregate UTC import failed before connection |
| Revision 12 focused handoff contracts | `Tests/CoopAutomationRunner.ContractTests` | Passed in Windows PowerShell `5.1.26100.9168` and PowerShell `7.6.4`, including JSON `DateTime`, shifted-time rejection, parent/launch-operation retention, and optional-watchdog evidence |
| Revision 12 canonical inventory | `m2d-c-01` | Passed all 22/22 contract projects; no product process launched |
| Clean Revision 12 live rerun | `m2d-live-r2-01` | Dedicated bootstrap, seven acknowledgements, UDP ownership, schema-v4 client identity, exact loaded hashes, unchanged protected result, and graceful automatic cleanup passed; client join ended at `RequestExpired` because the driver resolved `NetworkMain` from the wrong assembly |
| Fresh published-revision build | `m2d-r13-pub-b1` | Client and dedicated compile-only builds passed with installed inventories unchanged; selected client SHA-256 `1C500...79250`; 620/620 decompiled source files matched the earlier candidate despite generated reference-path metadata differences |
| Corrected-client transaction | `m2d-client-stage-r2` | Complete 32-file pre-image retained; only client DLL/PDB changed; installed fingerprint changed from `A53B...97F5` to `14DF...A0AF0`; dedicated trees and protected result remained unchanged |
| Corrected-client live rerun | `m2d-live-r3-01` | Exact client hash loaded; `NetworkMain.GameClient` resolved; state reached `WaitingForLobby`/`LobbyState=Idle`; stock `Not Logged In` screen and PID-native logs identified native platform login as the next prerequisite; exact client/dedicated cleanup was graceful and complete |
| Exact NetworkMain resolver contracts | `Tests/CoopAutomationJoin.ContractTests`, `m2d-r13-c1` | ILSpy located the installed type in `TaleWorlds.MountAndBlade.dll`; the focused test rejected the historical multiplayer-assembly mismatch, and the full canonical inventory passed 22/22 |
| Exact NetworkMain resolver compile-only | `%LOCALAPPDATA%\Temp\CoopSpectator\CompileOnly\networkmain-resolver-20260901` | Client and dedicated builds passed with zero errors; candidate client SHA-256 is `2437D2386E306C12154553D641B477E123A247938C9C2F944F870BB36F5D6887`; dedicated output and both installed module hashes remained unchanged |
| Native platform-login focused contracts | `Tests/CoopAutomationJoin.ContractTests`, `Tests/CoopAutomationRunner.ContractTests` | Exact `LobbyState` assembly/type/state/client identity, one-shot invocation, no retry while denied or already logging, non-lobby rejection, task outcomes, terminal status retention, and dedicated/client native-log routing passed; runner assertions passed in Windows PowerShell 5.1 and PowerShell 7 |
| Native platform-login canonical inventory | `m2e-contracts-20260901-03` | Passed all 22/22 contract projects after the last test update; no product process launched |
| Native platform-login compile-only proof | `m2e-compile-20260901-02` | Final client and dedicated builds passed after the last source-text update with installed inventories unchanged and no product process launch; client candidate SHA-256 is `6DE0B8AE1F38626FD11B7BDE59E86AD7AA106CD7E96596C7FECB2E137C4E8551`; dedicated output remained `BD328AAC4F2A64C28D3EDCE28BCE3D72FF164BDAF817D9460B302ED538702A78` |
| Native platform-login and server-list live proof | `m2e-live-r1-01` | Exact staged hashes, dedicated bootstrap, UDP ownership, one successful native login, `AtLobby`, 52 successful list responses, terminal-status retention, PID-log capture, protected-state preservation, and exact cleanup passed; no entry passed every selector and no join began |
| Map/unique-map selection correction | `e55f1bd`; focused join/runner contracts; `m2e1-c1` | Runner no longer substitutes `Map` for optional `UniqueMapId`; focused tests passed in both PowerShell engines where applicable; full inventory passed 22/22; no product process, build, installed mutation, or staging |
| Corrected-selection native join proof | `m2e1-live-r1-01` | Exact server selected; native join task accepted; actual GameNetwork client start, loopback rewrite, `InCustomGame`, `Join game successful`, server `CreatePlayer`, and visible `Awaiting Server` confirmed; formal status remained `JoinAccepted`, so the runner correctly returned `Timeout`; exact cleanup and protected-state preservation passed |
| GameNetwork handoff observer correction | `d1af692`; focused join contract; `m2e1-handoff-fix-contracts-20260901-01` | Actual `GameNetwork.StartMultiplayerOnClient(...)` patch now owns the post-rewrite notification; absent historical lobby signature no longer gates evidence; full inventory passed 22/22 |
| Timeout-status evidence correction | `3fdcda3`; focused runner contract; `m2e1-handoff-fix-contracts-20260901-01` | The last run/token-validated non-terminal status survives timeout into the aggregate report without changing `IsTerminal` or the timeout outcome; Windows PowerShell 5.1 and PowerShell 7.6.4 passed |
| Handoff correction compile-only proof | `m2e1-handoff-fix-compile-20260901-01` | Client and dedicated builds passed without a product launch or installed mutation; candidate client SHA-256 `049850693D98C300BAE2F5D297C1FD4F4A350E91854078D4AB103CCBE0B4ECF2`; dedicated output unchanged at `BD328AAC4F2A64C28D3EDCE28BCE3D72FF164BDAF817D9460B302ED538702A78` |
| Clean published handoff build | `m2e1-handoff-pub-compile-20260901-01` | Exact clean local/upstream `a7bd528`, `RepositoryDirty=false`, both builds passed, installed inventories unchanged, no product process launched; selected client DLL `7CC2D759...2977E97`, PDB `92D3135A...1A01B17`, dedicated unchanged at `BD328...02A78`; targeted ILSpy output for both changed patch types matched the pre-publication candidate exactly |
| Handoff-corrected client-only staging | `m2e1-handoff-stage-r1` | Installed only the clean DLL/PDB pair through a guarded whole-tree move; retained the complete prior 32-file tree; dedicated tree, protected result, Git state, product-process state, and ports remained unchanged; no rollback required; evidence class `ConfirmedPathHashOnly` |
| Handoff-corrected connection proof | `m2e1-handoff-live-r1-01` | Loaded exact client `7CC2...7E97` and dedicated `BD328...02A78`; observed post-rewrite `NetworkHandoff`, terminal `Connected`, native join success, and server `CreatePlayer`; returned `Pass`; exact graceful cleanup and protected-state preservation passed |

Contract coverage includes valid and invalid `RunId` values, run-token mismatch, loaded-module hash mismatch, expired and excessive-lifetime requests, exact and optional server filters, explicit separation of scene `Map` from serialized `UniqueMapId`, omission of an unavailable optional unique-map filter, no match, ambiguous match, terminal-state classification, strict atomic status replacement, exact defining-assembly resolution for `NetworkMain` and `LobbyState`, rejection of historical assembly/state/client mismatches, one-shot login/task outcome behavior, exclusive handoff notification ownership at the actual GameNetwork patch after address rewrite, terminal and last-valid non-terminal aggregate status retention, distinct client/dedicated PID-log evidence, and compilation of the bridge/driver/controller/console-command/lobby-patch source graph against narrow runtime stubs.

## 4. Requirement completion audit

| Requirement | Status | Implementation and validation evidence | Evidence class | Affected roles/scenarios | Documentation impact | Residual risk |
|---|---|---|---|---|---|---|
| SAF-004 default-off profile | `Partially Satisfied` | `ExperimentalFeatures.EnableTestAutomation`; bridge requires the complete run profile; isolated contract compilation passed | Source, contract test | Multiplayer client; battle-type independent | Architecture, build guide, risks, runtime flow updated | Full L2–L5 profile and runtime proof do not exist |
| RUN-001/003/004/005 narrow client protocol | `Satisfied for the connection slice` | Fixed `RunId` root; protocol 1.0/schema 3 identity; strict atomic status; launcher reuse rejection; `m2e1-handoff-live-r1-01` confirmed exact role registration, formal handoff, terminal connection, status retention, and exact cleanup | Source, contract test, runtime evidence | Multiplayer client; battle-type independent | Specification, implementation report, feasibility report, build guide, README, and risks updated | Broader Milestone 2B role heartbeat/progress requirements remain outside this narrow client protocol row |
| BLD-003/006 client identity slice | `Satisfied` | Launcher checks installed hash; clean published run produced `7CC2...7E97`; `m2e1-handoff-stage-r1` installed that exact DLL/PDB and retained the complete pre-image; `m2e1-handoff-live-r1-01` reported the same loaded hash | Source, contract test, compile-only evidence, controlled staging, runtime evidence | Multiplayer client | Build guide, staging report, and feasibility report updated | No campaign or battle identity claim follows from the connection-only proof |
| PROC-001 client launch slice | `Satisfied for the connection slice` | Immediate provisional identity, schema-v4 handoff, exact loaded hash, separate PID-log capture, formal connection, and graceful exact cleanup are live-proven in `m2e1-handoff-live-r1-01` | Source, contract test, runtime and staging evidence | Multiplayer client | Build guide, code map, feasibility report, and this report updated | Shared runtime locks and broader cancellation/crash recovery remain Milestone 2B gaps |
| CLI-002 initial machine prerequisite | `Partially Satisfied` | Launcher requires Steam in the current interactive session and records non-secret process IDs; `m2e-live-r1-01` proved successful native platform login and `AtLobby` in the current profile | Source, environment validation, runtime evidence | Multiplayer client | Build guide, feasibility report, and this report updated | Portability, anti-cheat, modal, account-expiry, and other-machine proof remain open |
| CLI-006 early feasibility gate | `Satisfied for the named machine profile` | `m2e1-handoff-live-r1-01` correlated exact dedicated lifecycle, UDP ownership, client identity, native login, exact server selection, native join, GameNetwork handoff, terminal `Connected`, server `CreatePlayer`, and cleanup | Source, contract test, runtime evidence | Multiplayer client and local dedicated server | Specification, feasibility report, and this report updated | Portability to other machine profiles remains unproved |
| CLI-008 run-scoped native-lobby intent | `Satisfied for the connection slice` | Default-off complete profile, exact owned-host gates, normal lobby APIs, post-rewrite notifier, terminal `Connected`, exact loaded identities, and protected-state preservation passed in `m2e1-handoff-live-r1-01` | Source, contract test, compile-only build, on-disk staging, runtime evidence | Multiplayer client and associated local dedicated server; battle-type independent | Specification Revision 16 and affected living documents updated | The vanilla bootstrap is not L2/L3 battle evidence |
| TST-004 safe default | `Satisfied` for this slice | No complete automation environment means the controller returns without work; notification remains gated by the complete default-off automation profile; the correction adds no production-active diagnostic path; the clean live connection proof used the explicit profile | Source, contract compilation, compile-only build, runtime evidence | Production multiplayer client | Specification and risks updated | Broader production-disabled regression coverage remains part of later milestone audits |

## 5. Remaining gates

Clean run `m2e1-handoff-live-r1-01` closed the client connection gate with exact loaded identity, `NetworkHandoff`, terminal `Connected`, native join success, unchanged protected result state, separate PID-log inventories, and exact graceful cleanup. No further identical connection rerun is required.

The project must still complete the broader Milestone 2B safety matrix before Milestone 3: live-role heartbeat/progress projections and distinct no-heartbeat/no-progress classification, exclusive shared-resource runtime locks, aggregate cancellation behavior, complete automated `Recover` contracts, and structured owned crash/modal/hang evidence. This client implementation still opens no campaign, cooperative mission, or battle and satisfies no L2/L3 row.
